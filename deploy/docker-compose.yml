services:
  web:
    build:
      context: ..
      dockerfile: ./deploy/dockerfile
      args: #variables de entorno para el dockerfile
        - usuario=chinchilla
    container_name: portfolio_web
    ports:
      - "8800:80"